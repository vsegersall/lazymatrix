% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsetting.R
\name{[,LazyMatrix,ANY,ANY,ANY-method}
\alias{[,LazyMatrix,ANY,ANY,ANY-method}
\title{Subset a LazyMatrix by columns}
\usage{
\S4method{[}{LazyMatrix,ANY,ANY,ANY}(x, i, j, ..., drop = TRUE)
}
\arguments{
\item{x}{A \code{LazyMatrix} object.}

\item{i}{Row index. Must be missing as row subsetting is not yet supported.}

\item{j}{Column index. Either a single integer returning a \code{LazyColumn},
or a vector of integers returning a \code{LazyMatrix}.}

\item{...}{Additional arguments (ignored).}

\item{drop}{Logical. Currently ignored.}
}
\value{
A \code{LazyColumn} if a single column is selected, or a
\code{LazyMatrix} if multiple columns are selected.
}
\description{
Subsets a \code{LazyMatrix} object by columns, returning either a
\code{LazyColumn} or a new \code{LazyMatrix} depending on the number
of columns selected. Row subsetting is not yet supported.
}
\examples{
A <- Matrix::sparseMatrix(i = c(1,2,3), j = c(1,2,3), x = c(1,2,3))
X <- LazyMatrix(A, "sd", "mean")

# Single column → LazyColumn
col <- X[, 2]

# Multiple columns → LazyMatrix
sub <- X[, 2:4]

}
