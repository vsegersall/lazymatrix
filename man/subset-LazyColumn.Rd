% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsetting.R
\name{subset-LazyColumn}
\alias{subset-LazyColumn}
\alias{[,LazyColumn,ANY,ANY,ANY-method}
\title{Subset a LazyColumn}
\usage{
\S4method{[}{LazyColumn,ANY,ANY,ANY}(x, i, j, ..., drop = TRUE)
}
\arguments{
\item{x}{A \code{LazyColumn} object.}

\item{i}{Index: positive integers, negative integers, logical vector,
character vector (if named), zero, or missing (nothing).}

\item{j}{Not used for \code{LazyColumn} (1-dimensional). Must be missing.}

\item{...}{Additional arguments (ignored).}

\item{drop}{Logical. Currently ignored — single element extraction always
returns a plain scaled numeric, consistent with vector subsetting.}
}
\value{
A \code{LazyColumn} when multiple elements are selected, or a
scaled numeric value when a single element is selected.
}
\description{
Subsets a \code{LazyColumn} object using standard R indexing rules:
positive integers, negative integers, logical vectors (with recycling),
character vectors (if named), zero, or missing (nothing).
}
\examples{
set.seed(123)
mat_a <- matrix(rnorm(500), nrow = 50, ncol = 10)
lazy_a <- LazyMatrix(mat_a, "sd", "mean")
lazy_c <- lazy_a[, 2]

# Single element → plain scaled numeric
lazy_c[2]

# Multiple elements → LazyColumn
lazy_c[c(1, 3, 5)]

}
